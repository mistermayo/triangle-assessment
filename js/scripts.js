var triangle = function(length1, length2, length3) {
  var numbers = [];
  var length1 = numbers[0];
  var length2 = numbers[0];
  var length3 = numbers[0];

  var triangle = [];
  // var equilateral = {
  //   length1: numbers[0], length2: numbers[0], length3: numbers[0]
  // }
  // var isosceles = {
  //   length1: [0], length2: [0], length3: [0]
  // }
  // var scalene = {
  //   length1: [0], length2: [0], length3: [0]
  // }

  if (length1 === length2 ||  length1 === length3) {
        triangle.push("equilateral").toString();
        return triangle

  };
};
